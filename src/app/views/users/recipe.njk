{% extends "layout.njk" %}
{% block content %}
<section class="contentReceita conteiner">
    <img class="homeReceita" src="{{ recipes.image }}" alt="Imagem de {{recipes.title}}">
    <div class="informations">
        <h2>{{ recipes.title }}</h2>
        <span>{{ recipes.author }}</span>
    </div>
</section>
<section class="conteiner bodyReceita">
    <div id="titleMore">
        <h2>Ingredientes</h2>
        <a class="btn-collapse">ESCONDER</a>
    </div>
    <div class="info_Ingredientes details">
        <ul>
            {% for items in items.ingredients %}
            <li>
                {{ items }}
            </li>
            {% endfor %}
        </ul>
    </div>
    <div id="titleMore">
        <h2>Modo de preparo</h2>
        <a class="btn-collapse">MOSTRAR</a>
        <div class="details">
        <p></p>
    </div>
    </div>

    <div id="titleMore">
        <h2>Informações adicionais</h2>
        <a class="btn-collapse">ESCONDER</a>
    </div>
    <div class="details hide-details">
        <p>{{ recipes.information }}</p>
    </div>
</section>
{% endblock %}
{% block scripts %}
<script src="/scripts.js"></script>
{% endblock %}